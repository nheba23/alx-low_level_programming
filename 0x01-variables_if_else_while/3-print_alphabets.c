#include <stdio.h>
/**
 * *main - Prints alphabets in lowercase and uppercase
 * Return: Always 0
 */
int main(void)
{
	int n = 9;

	while
		(n <= 60)
	{
			putchar(n);
			n++;
	}
	putchar('\n');
	return (0);
}
